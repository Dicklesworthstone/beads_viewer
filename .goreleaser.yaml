version: 2

project_name: bv
dist: /tmp/bv-dist
builds:
  - main: ./cmd/bv
    binary: bv
    ldflags:
      - -s -w
      - -X github.com/Dicklesworthstone/beads_viewer/pkg/version.version=v{{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    # Versioned names (#195): bv_<version>_<os>_<arch>.<ext>, so `gh release
    # download` users can tell releases apart by file name. The updater and
    # install.sh accept both this and the previous unversioned form.
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

# Homebrew tap publishing
brews:
  - name: bv
    repository:
      owner: Dicklesworthstone
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: https://github.com/Dicklesworthstone/beads_viewer
    description: "Graph-aware task management TUI for beads projects"
    custom_block: |
      license :cannot_represent
    skip_upload: true
    install: |
      bin.install "bv"
    test: |
      system "#{bin}/bv", "--version"
    caveats: |
      To get started with bv:
        1. Initialize beads in your project: br init
        2. Launch the TUI: bv

      For AI agent integration, use --robot-* flags:
        bv --robot-triage     # Get prioritized recommendations
        bv --robot-next       # Get single next action

# Scoop bucket publishing
scoops:
  - name: bv
    repository:
      owner: Dicklesworthstone
      name: scoop-bucket
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/Dicklesworthstone/beads_viewer
    description: "Graph-aware task management TUI for beads projects"
    license: https://raw.githubusercontent.com/Dicklesworthstone/beads_viewer/main/LICENSE
    skip_upload: true
